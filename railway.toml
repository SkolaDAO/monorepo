# Default config - each service should override with its own settings
# See apps/*/Dockerfile for individual service configs

[build]
builder = "dockerfile"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
